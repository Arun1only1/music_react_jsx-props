<!-- JSX -->

<!-- Javascript XML -->

```
1.must return only one div or fragment
2. we can use react fragment instead of div
 react fragment groups elements without adding extra node
 3. components can be nested... one component can include many other components
 4. properties are written using camel case => objectFit, onClick, onHover,className
```
